package model;

//Represents the possible concerns that a user looking for skin products may have. Default represents no
// user selection.
public enum ConcernType {
    ACNE, DRYNESS, HYPERPIGMENTATION, REDNESS, DEFAULT
}
